# Tasks: v1-deterministic-crash-matrix

## Phase 1: Preconditions
- [x] T001 Verify latest worktree state with `git status --short`, `git rev-parse HEAD`, and file existence checks for `src/storage.rs`, `tests/wal_recovery.rs`, `docs/file_format.md`, and `scripts/verify`.
- [x] T002 Confirm `spec.md` and `contracts.md` are unchanged before implementation; stop if canonical inputs changed incompatibly.
- [x] T003 Run or inspect current `cargo test --test wal_recovery` behavior to preserve existing regression coverage.

## Phase 2: Red Matrix Tests
- [x] T004 Create `tests/crash_matrix.rs` with shared CLI, temp path, WAL path, SQL record, WAL frame, stdout/stderr, cleanup, and report-case helper structs.
- [x] T005 Add CM-001 test for `pre-wal-append` using `seed_committed_one`; assert row 2 is invisible and WAL absent/empty is compatible.
- [x] T006 Add CM-002 test for `partial-wal-frame`; assert incomplete tail is ignored/truncated and reopen exits 0.
- [x] T007 Add CM-003 test for `wal-frame-without-commit-marker`; assert uncommitted/non-commit frame is not visible and existing WAL recovery behavior is not weakened.
- [x] T008 Add CM-004 test for `committed-wal-before-data-apply`; assert first and second reopen show row 2 exactly once.
- [x] T009 Add CM-005 test for `recovery-interrupted-after-first-apply`; prefer fixture state with row 2 already applied and WAL frames for rows 2 and 3 retained.
- [x] T010 Add CM-006 test for `corrupt-tail-after-committed-frame`; use a deterministic incomplete/invalid-length trailing fragment after the committed row 2 frame, assert successful reopen, and do not use complete checksum-invalid or invalid-magic frames for this case.
- [x] T011 Add required `tests/fixtures/crash_matrix/` tracked fixture manifest/README or seed descriptors naming `seed_committed_one`, CM-001..CM-006, crash point labels, evidence IDs, and expected visible rows, even when WAL bytes are generated by test helpers.
- [x] T012 In CM-003, represent "commit marker absent" with current WAL state byte `0x02` (`WAL_STATE_ROLLED_BACK`) for row 2 and require the test/report to state this mapping.

## Phase 3: Minimal Implementation Support
- [x] T013 Run `cargo test --test crash_matrix` and identify failures.
- [x] T014 If CM-005 cannot be proven with fixture-only setup, add the smallest private/test-only recovery interruption hook; otherwise do not edit production code for hooks.
- [x] T015 If current WAL replay rejects CM-006's required successful incomplete/invalid-length trailing fragment, adjust replay narrowly within the documented compatibility model and keep existing storage-corruption behavior for complete invalid frames.
- [x] T016 Ensure no public CLI command, flag, stdout, stderr, or exit-code change is introduced.

## Phase 4: Verification Runner And Report
- [x] T017 Add `scripts/verify_crash_matrix` with repo-root resolution, strict shell options, `cargo test --test crash_matrix`, report generation from observed case results, and report content validation.
- [x] T018 Make `tests/crash_matrix.rs` emit per-case observed results under `target/crash_matrix/` or make `scripts/verify_crash_matrix` execute the same harness in report mode; either way, actual rows and exit status in the markdown report must come from the executed cases.
- [x] T019 Generate `target/crash_matrix/crash_matrix_report.md` with one section or table row per CM-001..CM-006 containing `case_id`, evidence id, reopen command, expected rows, actual rows, WAL/file-format assertion, and exit status.
- [x] T020 Make the script fail when the report or any required case/evidence identifier is missing, or when required actual rows/status fields are absent.

## Phase 5: Documentation
- [x] T021 Update `docs/file_format.md` with the crash matrix WAL compatibility note, or record exact existing section rationale in the implementation final report if no doc change is needed.
- [x] T022 Do not update `docs/cli_contract.md` for CM-006; the required path is successful reopen with no user-facing CLI output/error change. Update CLI docs/tests only if another implementation detail unexpectedly changes public behavior and must be escalated within scope.

## Phase 6: Required Verification
- [x] T023 Run `cargo test --test crash_matrix`.
- [x] T024 Run `./scripts/verify_crash_matrix`.
- [x] T025 Run `./scripts/verify`.
- [x] T026 Confirm existing `tests/wal_recovery.rs` coverage remains present and passing.
- [x] T027 Record verification evidence and artifact delta in the scheduler final report artifact.

## Traceability
| Requirement | Tasks |
|---|---|
| CM-001 pre-wal-append | T004, T005, T017, T019 |
| CM-002 partial-wal-frame | T004, T006, T015, T017, T019, T021 |
| CM-003 no commit marker | T004, T007, T012, T026 |
| CM-004 committed WAL before data apply | T004, T008, T017, T019 |
| CM-005 recovery interrupted after first apply | T004, T009, T014, T017, T019 |
| CM-006 corrupt tail after committed frame | T004, T010, T015, T019, T021, T022 |
| required fixture directory | T011 |
| report contract | T017, T018, T019, T020, T027 |
| broader verification | T023, T024, T025, T026 |
